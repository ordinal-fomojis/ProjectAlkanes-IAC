{{- range $name, $config := .Values.apps -}}
{{- if $config.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $name }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ordinal-fomojis/ProjectAlkanes-IAC
    targetRevision: HEAD
    path: kubernetes/{{ $name }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ $name }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
{{- end -}}
{{- end -}}
