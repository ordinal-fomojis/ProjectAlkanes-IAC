{{- range $config := .Values.environments -}}
{{- if $config.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $config.name }}-deployment
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $config.name }}-shovel-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ $config.name }}-shovel-app
    spec:
      restartPolicy: Always
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
      - name: {{ $config.name }}-shovel-be
        image: shovelacr.azurecr.io/shovel-be:0390b531366cd07052ec11b0cf0591202283302a
        env:
        - name: APP_ENV
          value: {{ $config.name }}
        - name: NODE_ENV
          value: production
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /api/health
            port: 8080
---
{{- end -}}
{{- end -}}
